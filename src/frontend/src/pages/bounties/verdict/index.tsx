import { AppBarTemplate } from "@pages/index";

export default function ClaimVerdictPage(props: {}) {
    return <AppBarTemplate>
        <ClaimVerdictContents />
    </AppBarTemplate>
}

function ClaimVerdictContents() {

    return <></>
}