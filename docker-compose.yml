version: '3.4'

services:
  sponsorkit:
    image: ghcr.io/sponsorkit/api:${DOCKER_TAG}
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    networks:
    - network
    ports:
    - "80:80"
    - "443:443"

networks:
  network: